PAGE:
	- 1 NEXT PAGE
SECONDARY:
	- 0 DOWN
	- 1 SELECT

MAIN(PAGE)
	- 0 MAINSECONDARY
MAIN(SECONDARY)
	OPTIONS:
		- "SYNC TIME (BT)"
			- SELECT: SYNC TIME OVER BT
		- "TIMER"
			- SELECT: TIMER(TAB)
		- "STOPWATCH"
			- SELECT: STOPWATCH(TAB)

TIMER(TAB):
	START:
		- 0 SET
		- 1 INCREASE
	AFTER TIMER SET:
		- 0 MAIN{PAGE}
		- 1 START/STOP

STOPWATCH(TAB):
	- 0 RESET & MAIN(PAGE)
	- 1 START/STOP
